---
tags: [pattern, translation, chinese]
date: 2026-05-22
sources:
  - raw/llm-wiki.md
status: stable
---

# LLM Wiki

一种使用 LLM 构建个人知识库的模式。

这是一份创意文档，设计目的是可以直接复制粘贴到你自己的 LLM Agent（例如 OpenAI Codex、Claude Code、OpenCode / Pi 等）中使用。它的目标是传达高层理念，而你的 Agent 会在与你协作的过程中构建出具体的实现细节。本仓库中的 wiki 插件就是该模式的一个实现。

## 核心理念

大多数人与 LLM 和文档的交互方式类似于 RAG：你上传一组文件，LLM 在查询时检索相关片段，然后生成答案。这种方式可行，但 LLM 每次回答问题时都要从零开始重新发现知识，没有积累。问一个需要综合五份文档的微妙问题，LLM 每次都必须找到并拼凑相关片段。没有任何东西被沉淀下来。NotebookLM、ChatGPT 文件上传以及大多数 RAG 系统都是这样工作的。

这里的思路不同。LLM 不是在查询时从原始文档中检索，而是**增量地构建和维护一个持久化的 wiki**——一个结构化的、相互链接的 markdown 文件集合，位于你和原始资料之间。当你添加新资料时，LLM 不会仅仅为后续检索建立索引。它会阅读资料、提取关键信息，并将其整合到现有 wiki 中——更新实体页面、修订主题摘要、标注新数据与旧论断的矛盾之处、强化或挑战不断演进的综合性结论。知识被编译一次，然后_持续保持更新_，而不是每次查询时重新推导。

这是关键区别：**wiki 是一个持久的、复利增长的知识产物。** 交叉引用已经建立，矛盾已经标注，综合结论已经反映了你读过的所有内容。每添加一份资料、每问一个问题，wiki 都会变得更加丰富。

你从不（或极少）亲自编写 wiki——LLM 负责编写和维护所有内容。你负责策展资料、引导分析方向、提出好问题、思考意义所在。LLM 负责所有繁杂工作——总结、交叉引用、归档和簿记，这些工作让知识库随时间推移真正有用。实际操作中，我一边开着 LLM Agent，一边开着 Obsidian。LLM 根据我们的对话进行编辑，我实时浏览结果——跟踪链接、查看图谱视图、阅读更新后的页面。Obsidian 是 IDE；LLM 是程序员；wiki 是代码库。

这可以应用于很多不同场景。举几个例子：

*   **个人**：追踪你自己的目标、健康、心理、自我提升——归档日记条目、文章、播客笔记，随时间构建关于自己的结构化图景。
*   **研究**：在数周或数月内深入某个主题——阅读论文、文章、报告，增量构建一个带有演进论点的综合性 wiki。
*   **读书**：边读边归档每一章，构建人物、主题、情节线索及其关联的页面。读完后你就有了一个丰富的伴读 wiki。想想像 [Tolkien Gateway](https://tolkiengateway.net/wiki/Main_Page) 这样的粉丝 wiki——数千个相互链接的页面，涵盖人物、地点、事件、语言，由志愿者社区历时多年构建。你可以在阅读过程中由 LLM 完成所有交叉引用和维护工作，个人构建出类似的东西。
*   **商业/团队**：由 LLM 维护的内部 wiki，输入来源包括 Slack 线程、会议记录、项目文档、客户通话。可能有人类在环审阅更新。wiki 保持最新是因为 LLM 承担了团队中没人愿意做的维护工作。
*   **竞争分析、尽职调查、旅行规划、课程笔记、爱好深度研究**——任何随时间积累知识并希望有序组织而非散乱堆放的场景。

## 架构

共有三层：

**原始资料**——你策展的源文档集合。文章、论文、图片、数据文件。这些是不可变的——LLM 从中读取但从不修改。这是你的事实来源。

**Wiki**——LLM 生成的 markdown 文件目录。摘要、实体页面、概念页面、对比、概览、综合。LLM 完全拥有这一层。它创建页面、在新资料到来时更新页面、维护交叉引用、保持一切一致。你阅读它；LLM 编写它。

**Schema**——一个文档（例如 Claude Code 的 CLAUDE.md 或 Codex 的 AGENTS.md），告诉 LLM wiki 的结构、遵循的约定，以及在摄入资料、回答问题或维护 wiki 时执行的工作流程。这是关键配置文件——它使 LLM 成为一个有纪律的 wiki 维护者，而非通用聊天机器人。你和 LLM 随时间共同演进这个文件，逐步摸索出适合你领域的方法。

## 操作

**摄入（Ingest）。** 你将新资料放入原始集合并告诉 LLM 处理它。一个示例流程：LLM 阅读资料、与你讨论关键要点、在 wiki 中写入摘要页面、更新索引、更新 wiki 中相关实体和概念页面的交叉引用（一份资料可能涉及 10-15 个 wiki 页面）、在日志中追加条目。我个人倾向于逐份摄入资料并保持参与——我阅读摘要、检查更新、引导 LLM 侧重哪些内容。但你也可以批量摄入多份资料而减少监督。由你决定开发适合自己风格的工作流程，并将其记录在 schema 中供后续会话使用。

**查询（Query）。** 你针对 wiki 提问。LLM 搜索相关页面、阅读它们，并综合出带引用的答案。答案可以根据问题采取不同形式——markdown 页面、对比表格、幻灯片（Marp）、图表（matplotlib）、画布。重要洞察：**好的答案可以作为新页面归档回 wiki。** 你要求的对比、分析、你发现的关联——这些都有价值，不应消失在聊天历史中。这样你的探索就像摄入的资料一样在知识库中复利增长。

**检查（Lint）。** 定期让 LLM 对 wiki 进行健康检查。查找：页面间的矛盾、被新资料取代的过时论断、没有入站链接的孤立页面、被提及但缺少独立页面的重要概念、缺失的交叉引用、可以通过网络搜索填补的数据空白。LLM 擅长建议新的研究问题和寻找新资料的方向。这使 wiki 在增长过程中保持健康。

## 索引与日志

两个特殊文件帮助 LLM（和你）在 wiki 增长过程中导航。它们服务于不同目的：

**index.md** 是面向内容的。它是 wiki 中所有内容的目录——每个页面列出链接、一行摘要，以及可选的元数据如日期或资料数量。按类别组织（实体、概念、资料等）。LLM 在每次摄入时更新它。回答查询时，LLM 先读取索引找到相关页面，然后深入阅读。这在中等规模（约 100 份资料、约数百个页面）下效果出奇地好，避免了基于嵌入的 RAG 基础设施的需求。

**log.md** 是按时间顺序的。它是发生了什么以及何时发生的追加式记录——摄入、查询、检查。一个有用的技巧：如果每个条目以一致的前缀开头（例如 `## [2026-04-02] ingest | Article Title`），日志就可以用简单的 Unix 工具解析——`grep "^## \[" log.md | tail -5` 给你最近 5 条记录。日志提供了 wiki 演进的时间线，帮助 LLM 了解最近做了什么。

## 可选：CLI 工具

在某些时候你可能想构建小工具帮助 LLM 更高效地操作 wiki。最明显的是 wiki 页面的搜索引擎——在小规模下索引文件就够了，但随着 wiki 增长你需要正式的搜索。[qmd](https://github.com/tobi/qmd) 是一个好选择：它是一个本地 markdown 文件搜索引擎，支持混合 BM25/向量搜索和 LLM 重排序，全部在设备上运行。它既有 CLI（LLM 可以通过 shell 调用它），也有 MCP 服务器（LLM 可以将其作为原生工具使用）。你也可以自己构建更简单的东西——LLM 可以帮你按需快速编写一个简单的搜索脚本。

## 技巧与窍门

*   **Obsidian Web Clipper** 是一个浏览器扩展，可以将网页文章转换为 markdown。对于快速将资料放入原始集合非常有用。
*   **本地下载图片。** 在 Obsidian 设置 → 文件与链接中，将"附件文件夹路径"设为固定目录（例如 `raw/assets/`）。然后在设置 → 快捷键中搜索"Download"找到"Download attachments for current file"并绑定快捷键（例如 Ctrl+Shift+D）。剪藏文章后按快捷键，所有图片就会下载到本地磁盘。这是可选的但很有用——它让 LLM 可以直接查看和引用图片，而不依赖可能失效的 URL。注意 LLM 无法一次性原生读取带内联图片的 markdown——变通方法是让 LLM 先读取文本，然后单独查看部分或全部引用的图片以获取额外上下文。有点笨拙但足够好用。
*   **Obsidian 的图谱视图** 是查看 wiki 形态的最佳方式——什么连接到什么，哪些页面是枢纽，哪些是孤岛。
*   **Marp** 是基于 markdown 的幻灯片格式。Obsidian 有对应插件。适合直接从 wiki 内容生成演示文稿。
*   **Dataview** 是一个 Obsidian 插件，可以对页面 frontmatter 运行查询。如果你的 LLM 在 wiki 页面中添加 YAML frontmatter（标签、日期、资料数量），Dataview 可以生成动态表格和列表。
*   Wiki 只是一个 markdown 文件的 git 仓库。你免费获得版本历史、分支和协作能力。

## 为什么这行得通

维护知识库的繁琐部分不是阅读或思考——而是簿记。更新交叉引用、保持摘要最新、标注新数据与旧论断的矛盾、维护数十个页面间的一致性。人类放弃 wiki 是因为维护负担的增长速度快于价值增长。LLM 不会厌倦、不会忘记更新交叉引用、可以一次操作 15 个文件。Wiki 保持维护是因为维护成本接近于零。

人类的工作是策展资料、引导分析方向、提出好问题、思考意义所在。LLM 的工作是其他一切。

这个理念在精神上与 Vannevar Bush 的 Memex（1945）相关——一个个人的、策展的知识存储，文档之间有关联路径。Bush 的愿景更接近这个想法，而不是万维网最终变成的样子：私有的、主动策展的，文档之间的连接与文档本身同样有价值。他无法解决的部分是谁来做维护。LLM 处理了这个问题。

## 说明

本文档有意保持抽象。它描述的是理念，而非具体实现。确切的目录结构、schema 约定、页面格式、工具链——所有这些都取决于你的领域、你的偏好和你选择的 LLM。上面提到的一切都是可选和模块化的——取有用的，忽略不需要的。例如：你的资料可能只有文本，所以完全不需要图片处理。你的 wiki 可能足够小，索引文件就够了，不需要搜索引擎。你可能不关心幻灯片，只想要 markdown 页面。你可能想要完全不同的输出格式集。正确的使用方式是与你的 LLM Agent 分享这份文档，然后一起协作实例化一个适合你需求的版本。这份文档的唯一职责是传达模式。你的 LLM 可以搞定其余部分。

## See Also

- [LLM Wiki (English)](./llm-wiki.md) — 英文原始资料文档